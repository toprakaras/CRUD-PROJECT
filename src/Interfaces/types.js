/**
 * Projede kullanılan kullanıcı veri tipleri
 */

/**
 * @typedef {Object} User
 * @property {string|number} id - Kullanıcının benzersiz kimliği.
 * @property {string} fullName - Kullanıcının tam adı.
 * @property {string} email - Kullanıcının e-posta adresi.
 * @property {string} phone - Kullanıcının iletişim numarası.
 * @property {string} company - Kullanıcının çalıştığı şirket/kurum.
 * @property {boolean} isLocal - Verinin LocalStorage'a ait olup olmadığını belirtir.
 */

export const InterfaceDummy = true; // Sadece klasörün boş kalmaması için
